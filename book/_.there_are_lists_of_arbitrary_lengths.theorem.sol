n- Nat
(ThereIs (List Nat)
  (l: (List Nat)
    (Equal Nat
      (List.length Nat l)
      (Nat.self n))))

For all `n` of type Nat,
there is a List of Nats `l`
such that the length of `l`
equals `n`
